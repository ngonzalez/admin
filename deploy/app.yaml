---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: k8s
spec:
  selector:
    matchLabels:
      run: app-debian-sid
  template:
    metadata:
      labels:
        run: app-debian-sid
    spec:
      volumes:
      - name: cgroup
        hostPath:
          path: /sys/fs/cgroup
          type: Directory
      - name: data
        persistentVolumeClaim:
          claimName: pv-claim
      containers:
      - name: app-debian-sid
        image: ngonzalez121/debian-sid
        ports:
        - name: ssh
          containerPort: 22
        - name: nginx
          containerPort: 8000
        securityContext:
          privileged: true
          capabilities:
            add: ["SYS_ADMIN"]
        volumeMounts:
        - name: cgroup
          mountPath: /sys/fs/cgroup:ro
        - name: data
          mountPath: /data
