---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: development
spec:
  selector:
    matchLabels:
      run: redis
  template:
    metadata:
      labels:
        run: redis
    spec:
      containers:
      - name: redis
        image: registry.gitlab.com/ngonzalez121/demo-app-database-org/redis
        resources:
          requests:
            memory: "500M"
            cpu: "0.5"
          limits:
            memory: "2G"
            cpu: "2"
        ports:
        - name: redis
          containerPort: 6379
      dnsConfig:
        nameservers:
          - 8.8.8.8
          - 8.8.4.4
        searches:
          - link12.ddns.net
        options:
          - name: ndots
            value: "2"
          - name: eth0
