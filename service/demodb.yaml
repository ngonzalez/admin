---
apiVersion: v1
kind: Service
metadata:
  name: demodb-loadbalancer
  namespace: development
spec:
  type: LoadBalancer
  selector:
    run: demodb
  ports:
  - port: 5432
    name: postgresql
    targetPort: 5432