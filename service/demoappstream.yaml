---
apiVersion: v1
kind: Service
metadata:
  name: demoappstream-loadbalancer
  namespace: development
spec:
  type: LoadBalancer
  selector:
    run: demoappstream
  ports:
  - port: 3000
    name: puma
    targetPort: 3000
