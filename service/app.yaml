---
apiVersion: v1
kind: Service
metadata:
  name: app-loadbalancer
  namespace: k8s
spec:
  type: LoadBalancer
  selector:
    run: app-debian-sid
  ports:
  - port: 22
    name: ssh
    targetPort: 22
  - port: 80
    name: nginx
    targetPort: 8000
